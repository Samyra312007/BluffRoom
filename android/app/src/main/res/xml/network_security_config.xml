<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security policy for BluffRoom.

  Production servers must use HTTPS (SERVER_URL=https://your-server.com) and
  cleartext stays blocked everywhere by default. The exceptions below exist
  purely for local development: the Android emulator reaches the host machine
  via 10.0.2.2, and physical devices can use an adb reverse tunnel.

  To test against a plain-HTTP server on your LAN, add its host here, e.g.:
      <domain includeSubdomains="false">192.168.1.50</domain>
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
